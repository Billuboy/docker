{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.25.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "0.9.5",
    "serve": "^13.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "serve": "cd /work/Projects/Local/Docker/client/ && serve build -p 5000",
    "serve-docker": "cd /usr/src/app/ && serve build -p 5000"
  }
}
